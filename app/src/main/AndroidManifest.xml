<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppML"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".LoadActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".MainActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.EscalasActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.EscalaDetalheActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.MinhasEscalasActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.MusicasActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.MusicaDetalheActivity" android:screenOrientation="sensorPortrait"/>
        <activity android:name=".views.MensalidadeActivity"
            android:exported="false" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />



</manifest>